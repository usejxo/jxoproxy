version: 1
backend:
  phases:
    build:
      commands:  
        - npm run build
frontend:
  phases:
    preBuild:
      commands:
    build:
      commands:
        - npm run start
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
