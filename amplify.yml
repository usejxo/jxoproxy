version: 1
applications:
  frontend:
    phases:
      preBuild:
        commands:
          - echo "Skipping npm install as dependencies are pre-installed"
      build:
        commands:
          - npm run start
      postBuild:
        commands:
          - echo "Post-build phase"
