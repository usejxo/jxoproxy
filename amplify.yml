version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
